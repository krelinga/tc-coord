{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run Mockery",
            "type": "shell",
            "command": "rm -r internal/mocks && mockery",
            "group": {
                "kind": "build",
            },
            "problemMatcher": []
        }
    ]
}